export module runner;

namespace ance::runner
{
    /**
     * Is able to run ance code.
     */
    class Runner
    {
        // todo: the runner should take an ast::Statement and using the ast visitor, execute it
        // todo: the runner should also have a method addFunction(std::string const& name, std::function<void()> const& function)
    };
}
