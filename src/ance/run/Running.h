#ifndef ANCE_RUN_RUNNING_H
#define ANCE_RUN_RUNNING_H

#include <memory>

namespace ance::run
{
    /**
     * Runs code.
     */
    class Running
    {
        // todo: has a method void run(std::unique_ptr<bbt::Statement> const& statement);
        // todo: the runner should also have a method addFunction(std::string const& name, std::function<void()> const& function)
    };
}

#endif
